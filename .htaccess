Options -MultiViews
RewriteEngine On

# 1. Map clean URL requests back to their physical .php file
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^/]+)$ $1.php [L]